export default class SubCategory {
    constructor(title, slug, image, category) {
        this.title = title;
        this.slug = slug;
        this.image = image;
        this.category = category;
    }
}

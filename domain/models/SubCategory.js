export default class SubCategory {
    constructor(_id, title, slug, image, category) {
        this._id = _id;
        this.title = title;
        this.slug = slug;
        this.image = image;
        this.category = category;
    }
}
